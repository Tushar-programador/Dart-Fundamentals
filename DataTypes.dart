void main() {
  int a = 0;
  print(a);
  double f = 0002.6000;
  // it trunacates the perciding and post-perciding Zeros from value
  print(f);
  print(f.toString().runtimeType);
  String s = "   Hello, World!   ";
  // it  doesn't removes the white spaces from the string
  print(s);
  String th = "123abc456";
  // it converts the string to integer
  // print(th.toInt().runtimeType);
}
